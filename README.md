# Site web — Kimusubi Aïkido Iaïdo Gignac

Site statique de l'association (fondée en 1981) — Gignac, Hérault.

- `index.html` : la page du site (HTML/CSS, aucune compilation nécessaire)
- `img/` : illustrations

## Hébergement
Déployé via Netlify (ou Cloudflare Pages) : commande de build vide, dossier de publication = racine.

## Mise à jour
Modifier `index.html` directement sur GitHub (bouton crayon), la mise en ligne est automatique une fois Netlify connecté au dépôt.
